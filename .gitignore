# ---- dependências e build ----
node_modules/
.pnpm-store/
.next/
out/
build/
dist/
*.tsbuildinfo

# ---- segredo: NADA de credencial no git ----
.env
.env.local
.env*.local
.env.production
*.pem
*.key

# ---- teste e cobertura ----
coverage/
.nyc_output/
playwright-report/
test-results/
blob-report/
.playwright/

# ---- ferramenta e SO ----
.vercel
.turbo
.DS_Store

# IDE state
.idea/
*.log
npm-debug.log*
pnpm-debug.log*

# ---- supabase local ----
supabase/.branches
supabase/.temp
supabase/.env

# worktrees locais — nunca versionados
.claude/worktrees/

# ponte do agente — a ferramenta recria a pasta sozinha em toda sessão.
# Entrou versionada por engano em 23bd741 e saiu do git em 11-ago-2026.
# Segunda trava em eslint.config.mjs: os hooks .js dela quebravam o lint.
.agentbridge/

# ---- DADO REAL DE PACIENTE — nunca entra no git (repo é PÚBLICO) ----
# `_material/` tem 327 arquivos do v1/v2; 3 deles (em **/amostras/) têm nome,
# prontuário, leito e prescrição de paciente real.
_material/
**/amostras/
90-PHI-LOCAL/

# `memory/` é gerado por ~/projetos/scripts/indices/build_sasi_index.py e NÃO é
# código: são 31 MB de índice de busca (`sasi_index.db`) mais o `MAPA-SASI.md`.
# Ignorar aqui não é arrumação, é contenção — medido em 14-ago-2026, o índice
# guarda o CONTEÚDO INTEIRO de 298 arquivos de `_material/`, das 3 amostras com
# dado de paciente real e do `.env.local`. Proteger só o arquivo original e
# deixar a CÓPIA passar anula a trava acima.
memory/

# Config de IDE/agente local e export clinico - nunca no git (05-set-2026)
/.codex/
/.zed/
/analysis/data/

# repos independentes, com remote proprio no GitHub
EXTRACAO-CLINICA-SASI/
AUTOMACAO-SASI/
